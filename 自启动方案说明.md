# Claude Code 对话历史管理系统 - 使用指南

## 快速开始

```bash
# 1. 运行启动脚本
start.bat

# 2. 选择是否设置开机自启动 (Y/N)
# 3. 选择是否打开浏览器 (Y/N)
# 4. 关闭窗口，服务在后台继续运行
```

---

## 服务管理

| 操作 | 命令 |
|------|------|
| **启动服务** | `start.bat` |
| **停止服务** | `stop_service.bat` |
| **取消自启动** | `uninstall_service.bat` |
| **访问地址** | http://localhost:13001 |

---

## start.bat 功能说明

运行 `start.bat` 后会自动：

1. ✓ 检查 Python 环境
2. ✓ 检查 Flask 依赖（缺失会自动安装）
3. ✓ 检查端口 13001 是否被占用
4. ✓ 检查项目文件完整性
5. ✓ 在后台启动服务
6. ✓ 可选设置开机自启动
7. ✓ 可选自动打开浏览器

---

## 三种自启动方案对比

| 方案 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **任务计划程序** | 简单、集成在 start.bat 中 | 需要登录桌面 | ⭐⭐⭐⭐⭐ |
| **启动文件夹** | 系统自带、可靠 | 需要手动创建快捷方式 | ⭐⭐⭐⭐ |
| **Windows服务** | 开机自动运行、无需登录 | 配置复杂 | ⭐⭐⭐ |

---

## 方案一：任务计划程序（推荐）✅

### 设置方法

运行 `start.bat`，当提示以下内容时输入 `Y`：

```
Enable auto-start on boot? (Y/N): Y
```

### 特点
- ✅ 集成在 start.bat 中，无需额外操作
- ✅ 用户登录后自动启动
- ✅ 后台运行，无窗口
- ✅ 一键取消：运行 `uninstall_service.bat`

---

## 方案二：启动文件夹

### 设置方法

```bash
# 1. 按 Win+R，输入 shell:startup 回车
# 2. 打开启动文件夹

# 3. 创建 start.bat 的快捷方式
# 右键 → 新建 → 快捷方式
# 位置: D:\03_DevData\Projects\plugins\chat-history\start.bat
```

### 特点
- ✅ 系统自带功能
- ✅ 可靠性高
- ⚠️ 需要登录桌面才启动

---

## 方案三：Windows 服务（高级）⚙️

### 设置方法

#### 使用 NSSM（推荐）

```bash
# 1. 下载 NSSM
# https://nssm.cc/download

# 2. 解压到 nssm 文件夹

# 3. 安装服务
cd D:\03_DevData\Projects\plugins\chat-history
nssm install ClaudeCodeChatHistory "C:\Python311\pythonw.exe" "D:\03_DevData\Projects\plugins\chat-history\scripts\web_server.py"

# 4. 设置自动启动
nssm set ClaudeCodeChatHistory Start SERVICE_AUTO_START

# 5. 启动服务
nssm start ClaudeCodeChatHistory
```

### 特点
- ✅ 开机自动启动（无需登录）
- ✅ 后台运行
- ✅ 可设置服务重启
- ⚠️ 配置较复杂

### 管理

```bash
# 停止服务
nssm stop ClaudeCodeChatHistory

# 卸载服务
nssm remove ClaudeCodeChatHistory

# 查看状态
nssm status ClaudeCodeChatHistory
```

---

## 配置说明

### 端口修改

如需修改端口（默认13001），编辑以下文件：

**scripts/web_server.py**
```python
if __name__ == '__main__':
    run_server(13001)  # 修改这里的端口号
```

**start.bat** 和 **stop_service.bat**
```bat
:13001  # 修改这里的端口
```

### Python 路径

如果 Python 安装路径不同，修改脚本中的：
```bat
python → C:\Python311\python.exe
pythonw → C:\Python311\pythonw.exe
```

查看 Python 路径：
```bash
where python
where pythonw
```

---

## 常见问题

**Q: 端口 13001 被占用怎么办？**
A: 运行 `stop_service.bat` 停止旧服务，或修改端口号。

**Q: 服务启动失败？**
A: 运行 `start.bat` 查看详细错误信息，会检查 Python、Flask 等依赖。

**Q: 如何确认服务在运行？**
A: 访问 http://localhost:13001，或在任务管理器中查找 pythonw.exe 进程。

**Q: 关闭窗口后服务停止了？**
A: 旧版本会有这个问题，新版本已改为后台运行，可以关闭窗口。

**Q: 如何更换端口？**
A: 修改 `scripts/web_server.py` 和 `start.bat` 中的端口号。

---

## 推荐方案

### 个人使用：方案一（任务计划程序）

```bash
# 首次运行（会提示设置自启动）
start.bat

# 管理
start.bat           # 手动启动
stop_service.bat    # 手动停止
uninstall_service.bat  # 卸载自启动
```

### 服务器/无人值守：方案三（Windows 服务）

```bash
# 使用 NSSM 安装
# 开机自动启动，无需登录
```

---

*选择适合你的方案，享受便捷使用！*
